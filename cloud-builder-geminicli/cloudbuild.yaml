steps:
  # Build the image
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build'
    args:
      - 'build'
      - '--tag=europe-docker.pkg.dev/coffee-plantation/workstation/geminicli-builder:latest'
      - '-f'
      - 'Dockerfile'
      - '.'

  # Smoke test: check version
  - name: 'europe-docker.pkg.dev/coffee-plantation/workstation/geminicli-builder:latest'
    id: 'smoke-test-gemini'
    args: ['--version']

  # Smoke test: check geminicli alias
  - name: 'europe-docker.pkg.dev/coffee-plantation/workstation/geminicli-builder:latest'
    id: 'smoke-test-geminicli'
    entrypoint: 'geminicli'
    args: ['--version']

images:
  - 'europe-docker.pkg.dev/coffee-plantation/workstation/geminicli-builder:latest'

timeout: '1200s'
